<Page x:Class="Senior_Project_WPF.MainChoice"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:Senior_Project_WPF"
      mc:Ignorable="d"
      Title="Intelligent Inventory Management and Sales Analytics System"
      Height="450" Width="900">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4"
                   Text="Welcome to The Intelligent Inventory Management and Sales Analytics System"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="DarkBlue"
                   TextAlignment="Center"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap"
                   Margin="20"/>

        <Button Grid.Row="2" Content="Log In"
                Width="120" Height="40"
                Click="LoginButon_Click"
                HorizontalAlignment="Left" Margin="202,17,0,18"/>

        <Button Grid.Row="2" Grid.Column="3" Content="Register"
                Width="120" Height="40"
                Click="RegisterButon_Click"
                HorizontalAlignment="Left" Margin="8,17,0,18"/>
    </Grid>
</Page>
